<h1>All restaurants</h1>

<h3><%= link_to "Add a new restaurant", new_restaurant_path %></h3>

<% @restaurants.each do |restaurant| %>
  <div class="restaurant">
    <h2><%= link_to restaurant.name, restaurant_path(restaurant) %></h2>
  </div>
<% end %>
